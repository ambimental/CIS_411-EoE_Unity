//WILL BE USED WHENEVER A CARD PILE NEEDS TO BE EXPANDED FOR EASIER VIEWING

using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.EventSystems;

public class Expandable : MonoBehaviour, IPointerClickHandler {

    // Use this for initialization
    void Start () {
		
	}

    //when the user clicks on th epile of cards, it expands everything in the panel so that it is larger an
    //easier to see
    public void OnPointerClick(PointerEventData eventData)
    {
     
    }
}
